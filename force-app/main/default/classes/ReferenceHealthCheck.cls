/**
 * Generic sample Apex class created only to prove the reference pipeline.
 * Replace or delete it when adapting the repository to your own project.
 */
public with sharing class ReferenceHealthCheck {
    public static String status() {
        return 'OK';
    }
}
